import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260722193000_add_website_navigation_publishing.sql"),"utf8");describe("navigation publisher",()=>{it("is role checked, revision checked and fixed",()=>{expect(sql).toContain("private.can_manage_website()");expect(sql).toContain("ERRCODE='40001'");expect(sql).not.toMatch(/EXECUTE\s+format/i)});it("publishes parents before children and retains bounded history",()=>{const first=sql.indexOf("parentId','') IS NULL");const second=sql.indexOf("parentId','') IS NOT NULL");expect(first).toBeGreaterThan(0);expect(second).toBeGreaterThan(first);expect(sql).toContain("OFFSET 20")});it("revokes public execution",()=>{expect(sql).toContain("REVOKE ALL ON FUNCTION public.publish_website_navigation")})});
